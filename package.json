{
  "private": true,
  "name": "viboplr-vinyl-deck",
  "version": "1.4.2",
  "description": "Vinyl Deck visualizer for Viboplr \u2014 presses the play queue onto a single-sided record.",
  "type": "module",
  "scripts": {
    "build": "tsc --noEmit && vite build && node scripts/finish-build.mjs",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "sync-types": "node scripts/sync-types.mjs",
    "harness": "vite --port 5199 --strictPort --open /dev/harness.html",
    "package": "npm run build && npm test && bash scripts/package.sh",
    "verify": "npm run typecheck && npm test && npm run build"
  },
  "devDependencies": {
    "jsdom": "^30.0.1",
    "typescript": "^7.0.2",
    "vite": "^8.2.1",
    "vitest": "^4.1.10"
  }
}
